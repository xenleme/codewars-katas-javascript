// Now you have to write a function called square that takes an argument and
// returns the square of it.

const square = (x) => { return Math.pow(x, 2) }

console.log(square(7056));
console.log(square(289));
console.log(square(9025));
